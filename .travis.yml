matrix:
  include:
    - language: python
      python: "3.6"
      script:
        - "./bin/download.py"
    - language: node_js
      node_js: "stable"
      cache:
        yarn: true
        directories:
          - node_modules
